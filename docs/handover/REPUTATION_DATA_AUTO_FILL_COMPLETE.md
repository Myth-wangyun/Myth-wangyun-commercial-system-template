# 口碑页面数据自动填充 - 完整方案

## 📊 数据表总览

口碑页面共有4个表格：

### 1. 年度核心数据看板汇总（按校区）
- **显示条件**：仅网络、新媒体、市场口碑、合作伙伴显示
- **口碑页面**：不显示此表（is3TableType=true）

### 2. 月度数据表（按月份）✅ 本次修复
| 字段 | 数据来源 | 说明 |
|------|---------|------|
| 计划收入 | 咨询师计划表汇总 | ✅ 自动获取 |
| 实际收入 | 手动输入 | ⚠️ 需要手动输入 |
| 计划招生 | 咨询师计划表汇总 | ✅ 自动获取 |
| **实际招生** | **咨询量明细表** | ✅ **本次新增自动获取** |
| **退费人数** | **咨询量明细表** | ✅ **本次新增自动获取** |
| **上门总量** | **咨询量明细表** | ✅ **本次新增自动获取** |
| **咨询总量** | **咨询量明细表** | ✅ **本次新增自动获取** |
| **电话量** | **咨询量明细表** | ✅ **本次新增自动获取** |
| 市场投入 | 市场成本表 | ✅ 自动获取（SEM/新媒体） |
| 报名转化率 | 自动计算 | 实际招生 ÷ 咨询总量 |
| 当面转化率 | 自动计算 | 实际招生 ÷ 上门总量 |
| 上门率 | 自动计算 | 上门总量 ÷ 咨询总量 |
| 招生成本 | 自动计算 | 市场投入 ÷ 实际招生 |

### 3. 咨询师汇总表✅ 本次修复
| 字段 | 数据来源 | 说明 |
|------|---------|------|
| 计划收入 | 咨询师计划表汇总 | ✅ 自动获取（12个月累加） |
| 实际收入 | 手动输入汇总 | ⚠️ 需要手动输入 |
| 计划招生 | 咨询师计划表汇总 | ✅ 自动获取（12个月累加） |
| **实际招生** | **咨询量明细表汇总** | ✅ **本次新增自动获取（12个月累加）** |
| **退费人数** | **咨询量明细表汇总** | ✅ **本次新增自动获取（12个月累加）** |
| **上门总量** | **咨询量明细表汇总** | ✅ **本次新增自动获取（12个月累加）** |
| **咨询总量** | **咨询量明细表汇总** | ✅ **本次新增自动获取（12个月累加）** |
| **电话量** | **咨询量明细表汇总** | ✅ **本次新增自动获取（12个月累加）** |
| 市场投入 | 手动输入 | ⚠️ 需要手动输入 |
| 报名转化率 | 自动计算 | 实际招生 ÷ 咨询总量 |
| 当面转化率 | 自动计算 | 实际招生 ÷ 上门总量 |
| 上门率 | 自动计算 | 上门总量 ÷ 咨询总量 |
| 招生成本 | 自动计算 | 市场投入 ÷ 实际招生 |

### 4. 咨询师月度明细表✅ 已修复（上一版本）
| 字段 | 数据来源 | 说明 |
|------|---------|------|
| 计划收入 | 咨询师计划表 | ✅ 自动获取 |
| 实际收入 | 手动输入 | ⚠️ 需要手动输入 |
| 计划招生 | 咨询师计划表 | ✅ 自动获取 |
| **实际招生** | **咨询量明细表** | ✅ **已自动获取** |
| **退费人数** | **咨询量明细表** | ✅ **已自动获取** |
| **上门总量** | **咨询量明细表** | ✅ **已自动获取** |
| **咨询总量** | **咨询量明细表** | ✅ **已自动获取** |
| **电话量** | **咨询量明细表** | ✅ **已自动获取** |
| 市场投入 | 手动输入 | ⚠️ 需要手动输入 |
| 费用投入 | 手动输入 | ⚠️ 需要手动输入（同市场投入） |

## 🔄 数据流程图

```
咨询量录入系统（咨询量明细表）
    ├─ 按月份统计 → API: getMonthlyCampusSummary
    │   └─ 填充到：月度数据表（Table 2）
    │
    └─ 按咨询师+月份统计 → API: getConsultantMonthlySummary
        ├─ 填充到：咨询师月度明细表（Table 4）
        └─ 12个月累加 → 填充到：咨询师汇总表（Table 3）
```

## 📝 实现细节

### 后端API

#### 1. 月度校区汇总API（已存在）
**端点**：`GET /api/v1/consult/consultation/stats/monthly-campus-summary`

**参数**：
```typescript
{
  年份: 2026,
  校区: "盛邦",
  数据类型: "口碑"
}
```

**返回**：
```json
{
  "success": true,
  "data": {
    "月度数据": [
      {"月份": 1, "咨询总量": 50, "上门量": 30, "报名量": 20, "退费人数": 2},
      {"月份": 2, "咨询总量": 48, "上门量": 28, "报名量": 18, "退费人数": 1},
      ...
    ]
  }
}
```

#### 2. 咨询师月度汇总API（本次新增）
**端点**：`GET /api/v1/consult/consultation/stats/consultant-monthly-summary`

**参数**：
```typescript
{
  年份: 2026,
  校区: "盛邦",
  数据类型: "口碑"
}
```

**返回**：
```json
{
  "success": true,
  "data": {
    "咨询师数据": {
      "张三": {
        1: {"咨询总量": 20, "上门量": 12, "报名量": 8, "退费人数": 1, "电话量": 18},
        2: {"咨询总量": 18, "上门量": 10, "报名量": 7, "退费人数": 0, "电话量": 16}
      }
    }
  }
}
```

### 前端实现

#### 修改文件
`frontend/pages/consult/002-campus-yearly-monthly-media-source/002-media-source-yearly/components/GenericSourceDashboard.tsx`

#### 修改内容

**1. 添加API调用**
```typescript
const [consultResult, staffingMap, marketCostData, monthlyPlanTotals, 
       consultantPlanMap, consultantMonthlyData, monthlyCampusData] = await Promise.all([
  // ... 其他API
  statsApi.getConsultantMonthlySummary({...}),  // 咨询师月度数据
  statsApi.getMonthlyCampusSummary({...}),      // 月度校区数据（新增）
])
```

**2. 月度数据表自动填充**
```typescript
const makeMonthlyRow = (月份, campus, isTotal) => {
  const monthStats = monthlyCampusData?.data?.月度数据.find(m => m.月份 === 月份)
  return {
    实际招生: monthStats?.报名量 ?? null,
    退费人数: monthStats?.退费人数 ?? null,
    上门总量: monthStats?.上门量 ?? null,
    咨询总量: monthStats?.咨询总量 ?? null,
    // ...
  }
}
```

**3. 咨询师汇总表自动填充**
```typescript
// 12个月数据累加
consultantData.forEach(row => {
  if (row.咨询师) {
    const consultantMonthlyStats = 咨询师数据[row.咨询师] || {}
    MONTHS.forEach(m => {
      const monthStats = consultantMonthlyStats[m]
      if (monthStats) {
        yearly实际招生 += monthStats.报名量 || 0
        yearly退费人数 += monthStats.退费人数 || 0
        yearly上门总量 += monthStats.上门量 || 0
        yearly咨询总量 += monthStats.咨询总量 || 0
        yearly电话量 += monthStats.电话量 || 0
      }
    })
  }
})
```

## ✅ 修复后的效果

### 自动填充的数据
- ✅ **月度数据表**：每月的咨询总量、上门总量、报名量、退费人数、电话量
- ✅ **咨询师汇总表**：每个咨询师年度累计的咨询总量、上门总量、报名量、退费人数、电话量
- ✅ **咨询师月度明细表**：每个咨询师每月的详细数据

### 仍需手动输入的数据
- ⚠️ **实际收入**：财务数据（所有表）
- ⚠️ **市场投入/费用投入**：市场成本数据（除月度数据表外）

### 自动计算的数据
- ✅ **报名转化率**：实际招生 ÷ 咨询总量
- ✅ **当面转化率**：实际招生 ÷ 上门总量
- ✅ **上门率**：上门总量 ÷ 咨询总量
- ✅ **招生成本**：市场投入 ÷ 实际招生

## 🔧 需要执行的修复步骤

### 步骤1：修复后端语法错误
✅ **已完成** - 修复 `func.case` 语法错误

### 步骤2：执行数据库迁移
⏳ **待执行** - 添加 `数据类型` 字段到 `咨询师月度计划数据` 表

**SQL脚本**：
```sql
ALTER TABLE consult."咨询师月度计划数据"
ADD COLUMN IF NOT EXISTS "数据类型" VARCHAR(50) DEFAULT '汇总';

-- 更新约束和索引...
```

**执行方法**：
1. 打开pgAdmin
2. 执行 `backend/migrations/add_data_type_column_to_consultant_plan.sql`

### 步骤3：重启后端服务
```powershell
cd d:\Documents\Desktop\1\qm-system\backend
python main.py --mode dev
```

### 步骤4：刷新前端页面
- 访问"校区年度月度媒体来源"
- 切换到"口碑"Tab
- 验证数据自动显示

## 📊 数据验证

### 验证点1：月度数据表
检查每月的实际招生、退费人数、上门总量、咨询总量是否自动显示

### 验证点2：咨询师汇总表
检查每个咨询师的年度累计数据是否正确

### 验证点3：咨询师月度明细表
检查每个咨询师每月的详细数据是否正确

### 验证点4：转化率计算
检查报名转化率、当面转化率、上门率是否正确计算

## 🐛 已知问题修复

### 问题1：func.case 语法错误
✅ **已修复** - 简化为 `COUNT(DISTINCT 电话)`

### 问题2：数据类型字段不存在
⏳ **待修复** - 需要执行数据库迁移

### 问题3：月度数据表数据缺失
✅ **已修复** - 添加 `getMonthlyCampusSummary` API调用

### 问题4：咨询师汇总表数据缺失
✅ **已修复** - 添加12个月数据累加逻辑

## 📚 相关文档

- 详细实现文档未保留在当前仓库
- `../guides/REPUTATION_AUTO_FETCH_QUICK_REFERENCE.md` - 快速参考
- `backend/migrations/add_data_type_column_to_consultant_plan.sql` - 数据库迁移脚本

---

**更新时间**：2026-02-04  
**版本**：v2.0  
**修复内容**：添加月度数据表和咨询师汇总表的自动数据填充
