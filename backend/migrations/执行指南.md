# 快速执行指南

## 问题
微信平台数据无法保存以下字段：
- 数据诊断结果均值
- 完播率
- 平均播放时长
- 3s以上播放率
- 推荐人数
- 留言条数

## 解决步骤

### 第一步：添加数据库字段

**选择以下任一方法：**

#### 方法A：双击运行批处理文件（最简单）
```
双击文件：D:\Documents\Desktop\1\qm-system\backend\migrations\run_add_wechat_fields.bat
```

#### 方法B：在数据库中执行SQL
1. 打开数据库管理工具（Navicat/MySQL Workbench）
2. 打开文件：`migrations/add_wechat_missing_fields.sql`
3. 执行SQL脚本

### 第二步：重启后端服务
重启后端服务以加载新的模型定义

### 第三步：测试
1. 打开前端页面：市场部免费推广 -> 数据看板 -> 微信平台
2. 输入数据到新字段
3. 点击"保存数据"
4. 刷新页面，确认数据已保存

## 已完成的修改

✅ 数据库模型已更新（`app/models/market/free_promotion_daily.py`）
✅ API接口已更新（`app/api/v1/market/free_promotion_daily.py`）
✅ 前端组件已更新（`WechatDashboard.tsx`）

## 需要执行的操作

⚠️ **只需要执行第一步：添加数据库字段**

其他代码修改已完成，只需要在数据库中添加这6个字段即可。

## 验证

执行以下SQL验证字段是否添加成功：

```sql
SELECT 
    COLUMN_NAME as '字段名',
    COLUMN_TYPE as '数据类型',
    COLUMN_COMMENT as '注释'
FROM information_schema.COLUMNS 
WHERE TABLE_SCHEMA = DATABASE()
AND TABLE_NAME = '市场部免费推广微信平台日度数据表'
AND COLUMN_NAME IN (
    '视频号数据诊断结果均值',
    '视频号完播率',
    '视频号平均播放时长',
    '视频号3s以上播放率',
    '公众号推荐数',
    '公众号留言数'
);
```

应该返回6行记录。

