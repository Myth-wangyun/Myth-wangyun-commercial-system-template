# ============================================
# 清美教育管理系统 - Backend依赖包
# Python 3.12+
# FastAPI框架 + PostgreSQL数据库
# ============================================

# ============================================
# Web框架
# ============================================
fastapi==0.115.6              # FastAPI框架 - RESTful API框架
uvicorn[standard]==0.32.1     # ASGI服务器 - 支持HTTP/WebSocket
python-multipart==0.0.12      # 文件上传支持 - 表单数据处理

# ============================================
# 数据库（PostgreSQL）
# ============================================
psycopg[binary]==3.3.2        # PostgreSQL数据库驱动 - psycopg3，原生UTF-8支持
SQLAlchemy==2.0.48           # ORM框架 - 数据库抽象层（2026-03-02 发布）
alembic==1.14.0              # 数据库迁移工具 - 版本控制和迁移

# ============================================
# 数据验证和配置
# ============================================
pydantic==2.12.5             # 数据验证 - 请求/响应数据验证
pydantic-settings==2.12.0    # 配置管理 - 环境变量和配置管理
email-validator==2.2.0       # 邮箱验证 - 邮箱格式验证

# ============================================
# 认证和安全
# ============================================
python-jose[cryptography]==3.3.0  # JWT Token - JWT编码/解码
passlib[bcrypt]==1.7.4            # 密码加密 - 密码哈希和验证
bcrypt==5.0.0                     # 密码哈希 - bcrypt算法实现
python-dotenv==1.0.1              # 环境变量管理 - .env文件支持

# ============================================
# 日期时间处理
# ============================================
python-dateutil==2.9.0.post0 # 日期处理 - 日期解析和计算
pytz==2024.1                 # 时区处理 - 时区支持（APScheduler依赖）

# ============================================
# 定时任务调度
# ============================================
APScheduler==3.11.2          # 定时任务调度 - 后台定时任务调度框架
tzlocal==5.3.1               # 本地时区 - 获取系统时区信息（APScheduler依赖）

# ============================================
# Excel处理
# ============================================
openpyxl==3.1.5              # Excel读写 - 现代Excel文件(.xlsx)处理
types-openpyxl==3.1.5.20250919  # Mypy stubs for openpyxl / Excel import-export paths
xlrd==2.0.1                  # 旧版Excel读取 - 旧版Excel文件(.xls)支持

# ============================================
# HTTP请求
# ============================================
httpx==0.28.1                # HTTP客户端 - 异步HTTP客户端
requests==2.32.5             # HTTP库 - 同步HTTP请求库

# ============================================
# 数据序列化
# ============================================
ujson==5.10.0                # 快速JSON序列化 - 高性能JSON处理

# ============================================
# 日志
# ============================================
loguru==0.7.3                # 日志库 - 现代化日志处理（可选，代码中使用标准logging）

# ============================================
# 工具库
# ============================================
python-slugify==8.0.1        # URL友好字符串 - 字符串转URL友好格式
pypinyin==0.50.0             # 拼音转换 - 中文转拼音（支持中文处理）
Pillow==11.0.0                # 图片处理 - 图片验证和处理（PIL）

# ============================================
# 开发工具（开发环境使用）
# ============================================
pytest==8.3.4                # 测试框架 - 单元测试和集成测试
pytest-cov==6.2.1            # 覆盖率统计 - backend/app 单测覆盖门槛
pytest-asyncio==0.24.0       # 异步测试 - 异步测试支持
black==24.10.0               # 代码格式化 - 代码自动格式化工具
flake8==7.1.1                # 代码检查 - PEP8代码风格检查
mypy==1.13.0                 # 类型检查 - 静态类型检查工具
pyrefly==0.55.0              # 类型检查 - 新一代 Python 静态分析
ruff==0.15.5                 # 代码检查 - 快速 lint / import 排序

# ============================================
# 已移除的依赖
# ============================================
# pymysql==1.1.1             # 已移除 - 迁移到PostgreSQL
# cryptography==46.0.3       # 已移除 - MySQL加密连接不再需要（python-jose已包含）

# ============================================
# 生产环境服务器（Windows Server 2019）
# ============================================
# hypercorn==0.17.2          # ASGI服务器 - Windows生产环境推荐（原生支持ASGI）
# waitress==3.0.0            # WSGI服务器 - Windows专用，稳定可靠（需要asgiref适配器）
# asgiref==3.8.1             # ASGI适配器 - 用于Waitress适配FastAPI（ASGI→WSGI）

# ============================================
# 可选依赖（按需安装）
# ============================================
# pandas==2.2.3              # 数据分析 - 需要C编译器，大数据处理时使用
# redis==5.2.0               # Redis缓存 - 缓存和会话存储
# celery==5.4.0              # 异步任务队列 - 后台任务处理
# prometheus-client==0.21.1  # 监控指标 - 应用监控和指标收集
